<?xml version="1.0" encoding="UTF-8"?>

<level note="abc123">
    <invisible>
        <![CDATA[
            // Ez pl. egy (haszontalan) segedfuggveny, amit a jatekosnak
            // felesleges latnia.
            function equals(a, b) {
                if(a===b) {
                    return true;
                } else {
                    return false;
                }
            }
        ]]>
    </invisible>

    <readonly>
        <![CDATA[                    
            for(var x=0; x<=10<; x++) {
                level.placeBrickAt(x, 0);
                level.placeBrickAt(x, 10);
            }
            
            for(var y=1; y<=9; y++) {
                level.placeBrickAt(0, y);
                level.placeBrickAt(10, y);
            }
        ]]>
    </readonly>

    <editable>
        <![CDATA[
            // Ide irhat be a jatekos valamilyen
            // kodot, es/vagy a meglevot modosithatja...
            var x = 4;
        ]]>
    </editable>

    <readonly>
        <![CDATA[                    
            for(var x=0; x<=10<; x++) {
                level.placeBrickAt(x, 0);
                level.placeBrickAt(x, 10);
            }
            
            for(var y=1; y<=9; y++) {
                level.placeBrickAt(0, y);
                level.placeBrickAt(10, y);
            }
        ]]>
    </readonly>
</level>
